databaseChangeLog:
  - changeSet:
      id: schema
      author: master
      preConditions:
        - onFail: MARK_RAN
        - not:
            tableExists:
              tableName: users
      changes:
        - sqlFile:
            path: classpath:db/changelog/sql/schema.sql
